a = grid.shape
# k = 3
# grid = grid.flatten()


# for i in range(k):
#     grid_new = grid
#     grid_new = grid_new.flatten()
#     last = grid[0,-1]
#     for j in range(grid.shape[1] -1):
#         grid[0, j+1] = grid_new[0,j]
#     grid[0,0] = last
# grid = np.array(grid)
# np.reshape(grid, (a,b))